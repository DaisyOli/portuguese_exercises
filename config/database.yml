# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 8 } %>
  # Aumentar o timeout para evitar que conexões expirem durante requisições longas
  timeout: 10000
  connect_timeout: 2
  checkout_timeout: 15
  variables:
    statement_timeout: '90s'

development:
  <<: *default
  database: practice_fr_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: practice_fr_test

# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
production:
  <<: *default
  database: practice_fr_production
  username: practice_fr
  password: <%= ENV["PRACTICE_FR_DATABASE_PASSWORD"] %>
