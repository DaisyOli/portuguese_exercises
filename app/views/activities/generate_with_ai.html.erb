<% content_for :head do %>
  <title>Gerar Atividade com IA · Practice-BR</title>
  <style>
    .pb-input:focus { border-color:var(--brand) !important; box-shadow:0 0 0 3px var(--brand-veil-2) !important; outline:none !important; }
    .example-pill { cursor:pointer; transition:all 0.12s; }
    .example-pill:hover { background:var(--brand-tint) !important; border-color:var(--brand-bright) !important; color:var(--brand-deep) !important; }

    @keyframes ai-spin {
      to { transform: rotate(360deg); }
    }
    @keyframes ai-pulse {
      0%, 100% { transform: scale(1); opacity: 1; }
      50%       { transform: scale(0.88); opacity: 0.75; }
    }
    @keyframes ai-fade-in {
      from { opacity: 0; transform: scale(0.96); }
      to   { opacity: 1; transform: scale(1); }
    }
    #ai-loading-msg {
      transition: opacity 0.35s ease;
    }
  </style>
<% end %>

<%# ── Modal de carregamento IA ── %>
<div id="ai-loading-overlay" style="
  display: none;
  position: fixed; inset: 0; z-index: 9999;
  background: rgba(15, 56, 38, 0.55);
  backdrop-filter: blur(6px);
  align-items: center; justify-content: center;">

  <div style="
    background: var(--surface); border-radius: 24px; padding: 48px 40px;
    max-width: 380px; width: 90%; text-align: center;
    box-shadow: 0 32px 80px rgba(0,0,0,0.22);
    animation: ai-fade-in 0.3s ease;">

    <%# Spinner com logo %>
    <div style="position: relative; width: 96px; height: 96px; margin: 0 auto 28px;">
      <%# Anel exterior girando %>
      <div style="
        position: absolute; inset: 0; border-radius: 50%;
        border: 3px solid var(--brand-tint);
        border-top-color: var(--brand);
        border-right-color: var(--brand-bright);
        animation: ai-spin 1.1s linear infinite;">
      </div>
      <%# Anel interior (contra-rotação sutil) %>
      <div style="
        position: absolute; inset: 8px; border-radius: 50%;
        border: 2px solid transparent;
        border-bottom-color: var(--action);
        animation: ai-spin 1.8s linear infinite reverse;">
      </div>
      <%# Logo centralizado %>
      <div style="position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;">
        <img src="/icons/logo-mark-transparent-64x64.png"
             style="width: 44px; height: 44px; animation: ai-pulse 2.2s ease-in-out infinite;"
             alt="">
      </div>
    </div>

    <%# Mensagem rotativa %>
    <p id="ai-loading-msg"
       style="font-family:'Raleway',sans-serif; font-size:1.02rem; font-weight:700;
              color:var(--brand-deep); margin:0 0 10px; min-height:2.2em; line-height:1.4;">
    </p>

    <p style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.8rem; color:var(--ink-faint); margin:0;">
      Isso costuma levar alguns segundos ✨
    </p>
  </div>
</div>

<script>
(function() {
  var messages = <%= raw t('ai_generation.fun_phrases_prompt').to_json %>;

  var overlay = document.getElementById('ai-loading-overlay');
  var msgEl   = document.getElementById('ai-loading-msg');
  var form    = document.querySelector('form[action*="generate_with_ai"]');
  if (!form || !overlay || !msgEl) return;

  form.addEventListener('submit', function() {
    overlay.style.display = 'flex';

    var idx = Math.floor(Math.random() * messages.length);
    msgEl.textContent = messages[idx];
    // a tela de espera continua desta frase, sem piscar
    sessionStorage.setItem('ai-fun-phrase-idx', String(idx));

    var interval = setInterval(function() {
      msgEl.style.opacity = '0';
      setTimeout(function() {
        idx = (idx + 1) % messages.length;
        msgEl.textContent = messages[idx];
        msgEl.style.opacity = '1';
        sessionStorage.setItem('ai-fun-phrase-idx', String(idx));
      }, 350);
    }, 3800);

    document.addEventListener('turbo:before-render', function() {
      clearInterval(interval);
    }, { once: true });
  });
})();
</script>

<div style="max-width:680px; margin:1.5rem auto 3rem; padding:0 1.5rem;">

  <%# Cabeçalho %>
  <div style="display:flex; align-items:center; gap:12px; margin-bottom:1.25rem;">
    <%= link_to activities_path,
          style: "font-family:'Plus Jakarta Sans',sans-serif; font-size:0.82rem; font-weight:600; color:var(--ink-soft); padding:7px 14px; border:1.5px solid var(--line); border-radius:8px; text-decoration:none; background:var(--surface); white-space:nowrap;" do %>
      ← Voltar
    <% end %>
    <span style="font-family:'Raleway',sans-serif; font-size:1.2rem; font-weight:700; color:var(--ink);">
      ✨ Gerar Atividade com IA
    </span>
  </div>

  <%# Flash alert %>
  <% if flash[:alert] %>
    <div style="background:var(--error-tint); border:1.5px solid var(--error-border); border-radius:10px; padding:14px 16px; margin-bottom:18px; font-family:'Plus Jakarta Sans',sans-serif; font-size:0.85rem; color:var(--error-deep);">
      <%= flash[:alert] %>
    </div>
  <% end %>

  <div style="background:var(--surface); border:1px solid var(--line); border-radius:20px; padding:32px 36px;">

    <%# Explicação %>
    <div style="background:linear-gradient(135deg,var(--brand-tint) 0%,var(--brand-mist) 100%); border:1.5px solid var(--brand-bright); border-radius:14px; padding:18px 22px; margin-bottom:28px;">
      <div style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.85rem; font-weight:700; color:var(--brand-deep); margin-bottom:6px;">
        Como funciona?
      </div>
      <p style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.83rem; color:var(--brand); margin:0; line-height:1.6;">
        Descreva a atividade que você quer criar. A IA vai gerar título, descrição e questões automaticamente.
        Você vai <strong>revisar tudo</strong> antes de publicar — nada vai para os alunos sem sua aprovação.
      </p>
    </div>

    <%= form_tag generate_with_ai_activities_path, method: :post do %>

      <div style="margin-bottom:22px;">
        <label style="display:block; font-family:'Plus Jakarta Sans',sans-serif; font-size:0.82rem; font-weight:600; color:var(--ink); margin-bottom:8px;">
          Descreva a atividade que deseja criar
        </label>
        <%= text_area_tag :ai_prompt, params[:ai_prompt],
              id: "ai-prompt",
              class: "pb-input",
              placeholder: "Ex: Crie 5 questões de múltipla escolha sobre cumprimentos formais e informais em português brasileiro, nível A2. Use situações do dia a dia.",
              rows: 5,
              style: "width:100%; padding:13px 16px; border:1.5px solid var(--line); border-radius:12px; font-family:'Plus Jakarta Sans',sans-serif; font-size:0.9rem; color:var(--ink); background:var(--surface); resize:vertical; min-height:120px; box-sizing:border-box; line-height:1.6;" %>
        <div style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.75rem; color:var(--ink-faint); margin-top:6px;">
          Quanto mais detalhe você der (nível, tema, tipo de questão), melhor o resultado.
        </div>
      </div>

      <%# Exemplos rápidos %>
      <div style="margin-bottom:28px;">
        <div style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.75rem; font-weight:600; color:var(--ink-faint); margin-bottom:8px; text-transform:uppercase; letter-spacing:0.05em;">
          Exemplos de prompts
        </div>
        <div style="display:flex; flex-wrap:wrap; gap:8px;">
          <% [
            "5 questões de múltipla escolha sobre verbos reflexivos no cotidiano, nível B1",
            "6 exercícios de preencher lacunas com preposições de lugar, nível A2",
            "4 questões de múltipla escolha sobre gírias e expressões informais brasileiras, nível B2",
            "5 questões sobre o uso de 'por' e 'para', nível B1"
          ].each do |ex| %>
            <button type="button" class="example-pill"
                    onclick="document.getElementById('ai-prompt').value = '<%= j(ex) %>'"
                    style="font-family:'Plus Jakarta Sans',sans-serif; font-size:0.75rem; color:var(--ink-soft); background:var(--paper); border:1.5px solid var(--line); border-radius:20px; padding:5px 12px; text-align:left;">
              <%= ex %>
            </button>
          <% end %>
        </div>
      </div>

      <hr style="border:none; border-top:1px solid var(--line-soft); margin:0 0 24px 0;">

      <div style="display:flex; align-items:center; justify-content:space-between;">
        <%= link_to "Cancelar", activities_path,
              style: "font-family:'Plus Jakarta Sans',sans-serif; font-size:0.85rem; color:var(--ink-faint); text-decoration:none; padding:8px 4px;" %>
        <%= submit_tag "✨ Gerar atividade",
              style: "font-family:'Plus Jakarta Sans',sans-serif; font-size:0.9rem; font-weight:700; color:var(--surface); background:var(--brand); padding:12px 28px; border-radius:12px; border:none; cursor:pointer;" %>
      </div>

    <% end %>
  </div>
</div>
