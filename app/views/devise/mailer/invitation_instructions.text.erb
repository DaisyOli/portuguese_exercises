Practice-BR — Você foi convidado

<% teacher = @resource.invited_by %>
<% if teacher&.name.present? %>
<%= teacher.name %> te convidou para praticar português brasileiro na plataforma Practice-BR.
<% else %>
Você foi convidado para praticar português brasileiro na plataforma Practice-BR.
<% end %>

Aqui você vai encontrar atividades interativas criadas especialmente para o seu nível — vocabulário, compreensão de texto, preenchimento de lacunas e muito mais.

Para criar sua conta e começar a praticar, clique no link abaixo:

<%= accept_invitation_url(@resource, invitation_token: @token) %>

<% if @resource.invitation_due_at %>
Este convite expira em <%= l(@resource.invitation_due_at, format: :long) %>.
<% end %>

Se você não esperava este convite, pode ignorar este email com segurança. Nenhuma conta será criada.

---
© Practice-BR · Este é um email automático, por favor não responda.
